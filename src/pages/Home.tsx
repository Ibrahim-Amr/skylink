import Table from '../components/DataTable';

type Props = {};

const Home = (props: Props) => {
  return (
    <section className='py-20'>
      <Table />
    </section>
  );
};

export default Home;
